const initialState = {
    homes: []
}


// Action Types


// Reducer Function
function reducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}


// Action Creators - store.dispatch



export default reducer;